# Core Python Ignore
__pycache__/
*.py[cod]
*$py.class
*.so
.env
.venv
env/
venv/
.DS_Store
.vscode/
.idea/

# Output Directories (Exclude all generated data and logs)
chunk_output/
metadata_gen_output/
embeddings_output/
retrieval_output/
experiments/
logs/
evaluation/
embeddings_ablation/
embeddings_sensitivity/
retrieval_ablation/
retrieval_sensitivity/
retrieval_eval_final/

# Temporary Files and Logs
*.log
*.txt
# Allow requirements.txt
!requirements.txt

# Allow input_files folder structure but exclude content unless specified
input_files/*
!input_files/.gitkeep 
# (Add .gitkeep if needed, otherwise just ignore content)

# Project Specific Exclusions
# Markdown (Exclude all except README and diagrams)
*.md
!README.md
!paper_diagrams.md

# Evaluation Scripts and Non-Core Files
retriever_eval.py
retriever_gt.py
evaluate_*.py
reranker_eval.py
response_quality_eval.py
simple_rag_eval.py
fix_rag_eval.py
add_gpu_to_all.py
results_dump.txt
experiment_log.txt
query_analysis_log.txt
sample.txt
sample_q.json
gt.json
questions.json
settings.json
config.py  # Keep config.py? It's core configuration. I will keep it for now as it's cleaner. Wait, I added it to ignore list earlier. I'll remove it from ignore list so it's tracked.
!config.py

# Poster
Poster 2024.pdf
